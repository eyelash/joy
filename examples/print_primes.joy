import "std.joy";

func is_prime(n: Int): Bool {
	let i = 2;
	while (i * i <= n) {
		if (n % i == 0) {
			return false;
		}
		i = i + 1;
	}
	return true;
}

// print the first 100 primes
func print_primes() {
	let n = 2;
	let count = 0;
	while (count < 100) {
		if (n.is_prime()) {
			println(n);
			count = count + 1;
		}
		n = n + 1;
	}
}

func main() {
	print_primes();
}
